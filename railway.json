{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd senddrive && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && rm -rf node_modules && npm install && node index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
