{
  "name": "senddrive",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"cd server && node index.js\" \"cd senddrive && npm run dev -- --host\"",
    "build": "cd senddrive && npm install && npm run build",
    "start": "cd server && npm install && node index.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
